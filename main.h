#endif /* MAIN_H */

#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdio.h>
#include <unistd.h>

void customPrint(const char *format, ...);
int _printf(const char *format, ...);

#endif /* MAIN_H */
